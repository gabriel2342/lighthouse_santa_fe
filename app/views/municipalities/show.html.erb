<div class="container">
  <p style="color: green"><%= notice %></p>

  <%= render @municipality %>

  <div>
    <%= link_to "Edit this municipality", edit_organization_municipality_path(@organization, @municipality) %> |
    <%= link_to "Back to municipalities", organization_municipalities_path %>

    <%= button_to "Destroy this municipality", [@organization, @municipality], method: :delete %>
  </div>
</div>
