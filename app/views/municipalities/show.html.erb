<div class="container">
  <p style="color: green"><%= notice %></p>

  <%= render @municipality %>

  <div>
 <div>
    <%= link_to "Add Service Provider", new_organization_municipality_service_provider_path(@organization, @municipality), class:"btn btn-secondary" %>
  <div>
  <%= link_to "Edit this organization", edit_organization_path(@organization) %> |
  <%= link_to "Back to organizations", organizations_path %>

  <%= button_to "Destroy this organization", @organization, method: :delete %>
</div>