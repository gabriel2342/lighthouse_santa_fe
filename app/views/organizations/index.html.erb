  
  <div class="container bg-light w-50">
    <div class="container">
      <div class="container d-flex justify-content-between">

          <h1>Organizations </h1>
        
        <div class="container d-flex justify-content-end align-items-center">
          <%= turbo_frame_tag "new_org" do %>
            <%= link_to "Add Organization", new_organization_path, data: { turbo_frame: dom_id(Organization.new)}, class: "btn btn-primary" %>
          <% end %>
        </div>
      </div>
    </div>

    <%= turbo_frame_tag Organization.new %>
    <%= turbo_frame_tag "organizations" do %>
      <%= render @organizations%>
    <% end %>
  </div>
