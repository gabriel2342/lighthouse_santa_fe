<nav class="navbar navbar-expand-lg navbar-dark bg-dark pb-3">
  <div class="container-fluid">
    <%= link_to "Lighthouse", root_path, class:'navbar-brand' %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <%= link_to "About", about_path, class:'nav-link'%>
        </li>
         <li class="nav-item">
          <%= link_to "Contact", contact_path, class:'nav-link' %>
        </li>
      </ul>
          <% if user_signed_in? %>
              <%= link_to "Home", organizations_path, class: 'btn btn-secondary'%>   |     
              <%# <%= link_to "Municipalities FIX THIS", organizations_path, class: 'btn btn-secondary'%> 
              <%= button_to "Logout", destroy_user_session_path, method: :delete, class:'btn btn-danger'%>
          <% else %>
            <%= link_to "Sign in" , new_user_session_path, class:'btn btn-secondary' %>%
          <% end %>
    
    </div>
  </div>
</nav>